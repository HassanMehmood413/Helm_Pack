apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "proj_01_helm.fullname" . }}-test-connection"
  labels:
    {{- include "proj_01_helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "proj_01_helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
